import { fetchParams } from "../constant/variables.mjs";

export async function increaseLimit() {
  //  let foundItem;
  //  for await (const { pageItems } of myIterator) {
  //   foundItem = pageItems.find((item) => item.title.match(/Something/));
  //   if (foundItem) break;
  //  }
  //  console.log(foundItem);
}
